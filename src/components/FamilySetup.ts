export function mount(el: HTMLElement): void {
  el.innerHTML = `
    <div class="section-placeholder">
      <h2>Family Setup</h2>
      <p>Add your family members, kids and pets here.</p>
    </div>
  `
}
