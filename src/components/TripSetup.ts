export function mount(el: HTMLElement): void {
  el.innerHTML = `
    <div class="section-placeholder">
      <h2>Trip Setup</h2>
      <p>Plan your destination, dates, budget and holiday type here.</p>
    </div>
  `
}
